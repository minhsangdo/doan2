# Neo4j Configuration
NEO4J_URI=neo4j://127.0.0.1:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=sangdo54119

# OpenAI Configuration
OPENAI_API_KEY=AIzaSyBUoeOvN4LCB0_vcz7Wqv4wJhQibyw8MkI
OPENAI_MODEL=gpt-4o
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# App Configuration
APP_HOST=0.0.0.0
APP_PORT=8000
# Local: http://localhost:5173 | Hugging Face Space: URL đầy đủ của Space (nếu để trống, API dùng header Origin)
FRONTEND_URL=http://localhost:5173

# Quên mật khẩu — trên Hugging Face Space: bắt buộc dùng Resend (SMTP Gmail hay bị chặn/treo).
# Đăng ký https://resend.com → API Keys → dán vào RESEND_API_KEY (Secret trên HF).
RESEND_API_KEY=
# Gửi từ (mặc định của Resend khi chưa verify domain). Có domain riêng thì đổi theo hướng dẫn Resend.
RESEND_FROM=DNC Chatbot <onboarding@resend.dev>

# Chạy local: Gmail SMTP (2FA + App Password)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password_here

# Optional: Anthropic
ANTHROPIC_API_KEY=your_anthropic_api_key_here
